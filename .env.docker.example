# Copia este archivo a `.env` junto a docker-compose.yml y completa valores.
# NO lo commitees. Genera secretos con: openssl rand -hex 32

# Password de Postgres.
POSTGRES_PASSWORD=cambiame-por-algo-fuerte

# Secretos de la app: 32 bytes en hex (64 caracteres), uno por linea, distintos.
AUTH_SECRET=
WORKLOG_ENCRYPTION_KEY=

# Dominio para HTTPS automatico (Caddy + Let's Encrypt). Debe resolver a la IP
# de este servidor y tener 80/443 abiertos. Sin dominio propio, usa nip.io:
# para la IP 2.25.184.183 -> 2-25-184-183.nip.io
DOMAIN=2-25-184-183.nip.io

# Imagen promovida. En producción usar un tag Git SHA o, preferentemente, la
# referencia por digest real obtenida del registry. Nunca inventar el digest.
PULSO_IMAGE=pulso:local

# Solo si traes tu propio reverse proxy (comentando el servicio caddy):
# APP_PORT=3000

# IA Teams:
# Se configura por organizacion desde Pulso Admin -> IA de la organizacion.
# Sin config activa, el pulso usa resumen heuristico y las sugerencias usan mock.
# Registro y APIs Personal permanecen cerrados en producción.
PULSO_PUBLIC_REGISTRATION_ENABLED=false
PULSO_PERSONAL_API_ENABLED=false
PULSO_MARKETING_ORIGIN=https://pulso.example.com

# Coincidencia exacta de host o host:puerto. Toda URL LLM privada requiere una
# entrada explícita en la allowlist privada.
LLM_ALLOWED_PUBLIC_HOSTS=
LLM_ALLOWED_PRIVATE_HOSTS=

# Gmail y consultas web (el destinatario puede coincidir con el remitente).
GOOGLE_GMAIL_SENDER=
PULSO_CONTACT_RECIPIENT=
